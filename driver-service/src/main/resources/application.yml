spring:
  application:
    name: driver-service

server:
  port: 8764

eureka:
  server: 8761
  client:
    service-url:
      defaultZone: http://localhost:${eureka.server}/eureka
  instance:
    prefer-ip-address: true